version: 3
repos:
- id: /.*/
  allow_custom_workflows: true
workflows:
  default:
    plan:
      steps:
      - run: rm -rf .terraform
      - init:
          extra_args: [-backend-config="/test/terraform.tfstate"]