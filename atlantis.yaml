version: 3
workflows:
  default:
    plan:
      steps:
      - run: 
          command: rm -rf .terraform
          output: show
      - init:
          extra_args: [-backend-config="key=test/terraform.tfstate"]